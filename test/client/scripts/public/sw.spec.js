
'use strict';

import 'chai';

describe('Service Worker', () => {
  // no serviceWorker in navigator - how to test?
  /*
  it('should register a service worker and cache files on install', () =>
    navigator.serviceWorker.register('../../../../public/js/sw.bundle.js')
      .then(registration => {
        console.log(registration);
      })
  );
  */
});
