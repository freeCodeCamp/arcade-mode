
/* eslint spaced-comment: 0 */
/* eslint no-redeclare: 0 */
/* eslint no-undef: 0 */
/* eslint no-unused-vars: 0 */

const assert = require('chai').assert;

/// title: Program termination
/// type: rosetta-code

/// categories:
/// Simple

/// difficulty: ?

/// benchmark:
replaceWithActualFunctionHere;

/// description:
/// <br>
/// <br>
/// <br>
/// <br>
/// <dl class="rosetta__description-list"><dt class="rosetta__description-title">Task:</dt></dl>
/// Show the syntax for a complete stoppage of a program inside a &nbsp; <a class="rosetta__link--rosetta" href="http://rosettacode.org/wiki/Conditional Structures" title="Conditional Structures">conditional</a>. 
/// <br>
/// This includes all <a class="rosetta__link--rosetta" href="http://rosettacode.org/wiki/threads" title="threads">threads</a>/<a class="rosetta__link--rosetta" href="http://rosettacode.org/wiki/processes" title="processes">processes</a> which are part of your program.
/// <br>
/// Explain the cleanup (or lack thereof) caused by the termination (allocated memory, database connections, open files, object finalizers/destructors, run-on-exit hooks, etc.).  
/// <br>
/// Unless otherwise described, no special cleanup outside that provided by the operating system is provided.
/// <br><br>
/// <br>
/// <br>

/// challengeSeed:
function replaceMe (foo) {
  // Good luck!
  return true;
}

/// solutions:
if (some_condition) 
    quit();

/// rawSolutions:
=={{header|JavaScript}}==
{{works with|SpiderMonkey}}
The <code>quit()</code> function exits the shell.
<lang javascript>if (some_condition) 
    quit();</lang>




/// tail:
const replaceThis = 3;

/// tests:
assert(typeof replaceMe === 'function', 'message: <code>replaceMe</code> is a function.');
