
/* eslint spaced-comment: 0 */
/* eslint no-redeclare: 0 */
/* eslint no-unused-vars: 0 */

const assert = require('chai').assert;

/// title: Problem 412: Gnomon numbering
/// type: project-euler

/// categories:
/// math

/// difficulty: ?

/// benchmark:
//replaceWithActualFunctionHere;

/// description:
/// <div class="euler"><p class="euler__paragraph">For integers m, n (0 ≤ n < m), let L(m, n) be an m×m grid with the top-right n×n grid removed.</p><p class="euler__paragraph"></p><p class="euler__paragraph">For example, L(5, 3) looks like this:</p><p class="euler__paragraph"></p><p class="euler__paragraph"></p><p class="euler__paragraph"></p><p class="euler__paragraph">We want to number each cell of L(m, n) with consecutive integers 1, 2, 3, ... such that the number in every cell is smaller than the number below it and to the left of it.</p><p class="euler__paragraph"></p><p class="euler__paragraph">For example, here are two valid numberings of L(5, 3):</p><p class="euler__paragraph"></p><p class="euler__paragraph"></p><p class="euler__paragraph">Let LC(m, n) be the number of valid numberings of L(m, n).</p><p class="euler__paragraph">It can be verified that LC(3, 0) = 42, LC(5, 3) = 250250, LC(6, 3) = 406029023400 and LC(10, 5) mod 76543217 = 61251715.</p><p class="euler__paragraph"></p><p class="euler__paragraph">Find LC(10000, 5000) mod 76543217.</p></div>

/// challengeSeed:
function euler412() {
  // Good luck!
  return true;
}

euler412();

/// solutions:

/// tail:
const replaceThis = 3;

/// tests:
assert.strictEqual(euler412(), 38788800, 'message: <code>euler412()</code> should return 38788800.');
/// id: 5900f5081000cf542c51001a
